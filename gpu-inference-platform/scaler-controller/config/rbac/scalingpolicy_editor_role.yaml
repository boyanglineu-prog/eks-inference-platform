# This rule is not used by the project scaler-controller itself.
# It is provided to allow the cluster admin to help manage permissions for users.
#
# Grants permissions to create, update, and delete resources within the autoscaling.example.com.
# This role is intended for users who need to manage these resources
# but should not control RBAC or manage permissions for others.

apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: scaler-controller
    app.kubernetes.io/managed-by: kustomize
  name: scalingpolicy-editor-role
rules:
- apiGroups:
  - autoscaling.example.com
  resources:
  - scalingpolicies
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - autoscaling.example.com
  resources:
  - scalingpolicies/status
  verbs:
  - get
